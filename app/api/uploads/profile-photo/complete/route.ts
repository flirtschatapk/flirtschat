import {NextResponse} from "next/server";
import {createClient} from "@/lib/supabase/server";
export async function POST(request:Request){const supabase=await createClient(),{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const body=await request.json().catch(()=>null) as {objectKey?:string}|null;if(!body?.objectKey||!body.objectKey.startsWith(`profiles/${user.id}/`))return NextResponse.json({error:"Invalid object key"},{status:400});const {data:existing}=await supabase.from("fc_profile_photos").select("id,position").eq("user_id",user.id).order("position");const used=new Set((existing??[]).map(x=>x.position)),position=[0,1,2,3,4,5].find(x=>!used.has(x));if(position===undefined)return NextResponse.json({error:"Maximum six photos"},{status:409});const {data,error}=await supabase.from("fc_profile_photos").insert({user_id:user.id,object_key:body.objectKey,position,moderation_status:"pending"}).select("id").single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({id:data.id,status:"pending"})}
