"use client";
import {BadgeCheck,Bell,Crown,Gift,Heart,MessageCircle} from "lucide-react";import {useEffect,useState} from "react";import {AppBottomNav} from "@/components/app-bottom-nav";import {DesktopSidebar} from "@/components/dashboard/desktop-sidebar";import {ProfileImage} from "@/components/profile-image";import {createClient} from "@/lib/supabase/client";
type Match={id:string;profileId:string;name:string;username:string;online:boolean;verified:boolean;premium:boolean;created:string};
export function MatchesExperience(){const[items,setItems]=useState<Match[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{const supabase=createClient();void(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user){setError("Please sign in");setLoading(false);return}const{data,error:matchError}=await supabase.from("fc_matches").select("id,user_a,user_b,created_at").is("unmatched_at",null).or(`user_a.eq.${user.id},user_b.eq.${user.id}`).order("created_at",{ascending:false});if(matchError){setError(matchError.message);setLoading(false);return}const rows=data??[],ids=rows.map(x=>x.user_a===user.id?x.user_b:x.user_a),{data:profiles}=ids.length?await supabase.from("fc_profiles").select("id,display_name,username,last_seen_at,verified,premium").in("id",ids):{data:[]};setItems(rows.map(row=>{const id=row.user_a===user.id?row.user_b:row.user_a,p=profiles?.find(x=>x.id===id);return{id:row.id,profileId:id,name:p?.display_name||"Flirtschat member",username:p?.username||"member",online:p?Date.now()-new Date(p.last_seen_at).getTime()<300000:false,verified:Boolean(p?.verified),premium:Boolean(p?.premium),created:new Date(row.created_at).toLocaleDateString()}}));setLoading(false)})()},[]);return <main className="matches-page"><DesktopSidebar active="matches"/><div className="matches-shell"><header className="matches-top-header"><a className="matches-brand" href="/matches">FLIRTSCHAT</a><div><a className="matches-header-action premium" href="/premium"><Crown/></a><a className="matches-header-action notification" href="/notifications"><Bell/></a></div></header><div className="matches-tabs"><button className="active"><Heart/><span>All Matches</span><b>{items.length}</b></button></div><section className="matches-list">{loading?<p>Loading matches…</p>:error?<p>{error}</p>:items.length?items.map(person=><article className="match-person-card" key={person.id}><a className="match-person-avatar" href={`/profile/${person.profileId}`}><ProfileImage/><i className={person.online?"online":""}/></a><div className="match-person-copy"><a href={`/profile/${person.profileId}`}><h2>{person.name}{person.verified&&<BadgeCheck/>}</h2></a><p>@{person.username}</p><small>{person.created}</small></div>{person.premium&&<Crown/>}<a className="match-message-button" href={`/chats/${person.id}`}><MessageCircle/></a><a className="match-message-button" href={`/gifts?to=${person.profileId}`}><Gift/></a></article>):<p>No matches yet.</p>}</section></div><AppBottomNav active="matches"/></main>}
