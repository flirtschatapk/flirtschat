"use client";
import { zodResolver } from "@hookform/resolvers/zod";import { AnimatePresence,motion,useReducedMotion } from "framer-motion";import { ArrowLeft,ArrowRight,Check,LoaderCircle } from "lucide-react";import { useRouter } from "next/navigation";import { useEffect,useRef,useState } from "react";import { useForm } from "react-hook-form";import { onboardingDefaults,onboardingSchema,type OnboardingValues } from "@/lib/onboarding-schema";import { loadOnboarding,saveOnboarding } from "@/lib/onboarding-storage";import { isMockAuthenticated } from "@/lib/mock-auth-guard";import {mockUser} from "@/lib/mock-user";import { Logo } from "@/components/logo";import { AboutMeStep } from "./about-me-step";import { InterestsStep } from "./interests-step";import { OnboardingProgress } from "./onboarding-progress";import { PhotoUploadStep } from "./photo-upload-step";import { PreferencesStep } from "./preferences-step";
const fields:{[key:number]:(keyof OnboardingValues)[]}={1:["photos"],2:["bio","gender","interestedIn","dateOfBirth","country","city","languages"],3:["interests","relationshipGoal"],4:["minAge","maxAge","maxDistance","showMe","acceptedTerms"]};
export function OnboardingFlow(){const router=useRouter(),reduce=useReducedMotion();const [ready,setReady]=useState(false),[step,setStep]=useState(1),[direction,setDirection]=useState(1),[toast,setToast]=useState(false);const submitting=useRef(false);const form=useForm<OnboardingValues>({resolver:zodResolver(onboardingSchema),defaultValues:onboardingDefaults,mode:"onTouched"});useEffect(()=>{if(!isMockAuthenticated()){router.replace("/login");return}const saved=loadOnboarding();if(saved.completed){router.replace("/dashboard");return}setStep(saved.step);form.reset({...saved.data,displayName:saved.data.displayName||mockUser.displayName});setReady(true)},[form,router]);useEffect(()=>{if(!ready)return;const sub=form.watch(data=>saveOnboarding({step,completed:false,data:{...onboardingDefaults,...data} as OnboardingValues}));return()=>sub.unsubscribe()},[form,ready,step]);const next=async()=>{if(!(await form.trigger(fields[step],{shouldFocus:true})))return;const nextStep=Math.min(4,step+1);saveOnboarding({step:nextStep,completed:false,data:form.getValues()});setDirection(1);setStep(nextStep);scrollTo({top:0,behavior:reduce?"auto":"smooth"})};const back=()=>{setDirection(-1);setStep(s=>Math.max(1,s-1))};const skip=()=>{setDirection(1);setStep(s=>Math.min(4,s+1))};const finish=form.handleSubmit(async data=>{if(submitting.current)return;submitting.current=true;await new Promise(r=>setTimeout(r,900));saveOnboarding({step:4,completed:true,data});setToast(true);setTimeout(()=>router.replace("/dashboard"),650)});if(!ready)return <main className="route-loading"><LoaderCircle className="spin"/><span>Loading your profile…</span></main>;return <main className="onboarding-page"><div className="onboarding-hearts" aria-hidden="true"><i>♥</i><i>♥</i><i>✦</i></div><header className="onboarding-header"><Logo/><span>Make it unmistakably you.</span></header><div className="onboarding-layout"><aside><p className="kicker">Your profile, your vibe</p><h2>Great connections start with a great profile.</h2><p>Take a minute to show people what makes you, you. Everything can be edited later.</p><div className="profile-tip glass"><b>Profile tip</b><span>Profiles with 3+ photos get more meaningful conversations.</span></div></aside><div className="onboarding-main"><OnboardingProgress step={step}/><form onSubmit={finish} noValidate><AnimatePresence mode="wait" custom={direction}><motion.div className="onboarding-card glass" key={step} custom={direction} initial={reduce?false:{opacity:0,x:direction*24}} animate={{opacity:1,x:0}} exit={reduce?{opacity:0}:{opacity:0,x:direction*-24}} transition={{duration:.22}}>{step===1&&<PhotoUploadStep form={form}/>} {step===2&&<AboutMeStep form={form}/>} {step===3&&<InterestsStep form={form}/>} {step===4&&<PreferencesStep form={form}/>}</motion.div></AnimatePresence><div className="onboarding-actions">{step>1?<button className="onboarding-secondary" type="button" onClick={back}><ArrowLeft/> Back</button>:<span/>}{(step===2||step===3)&&<button className="onboarding-skip" type="button" onClick={skip}>Skip for now</button>}{step<4?<button className="onboarding-next" type="button" onClick={next}>Continue <ArrowRight/></button>:<button className="onboarding-next" disabled={form.formState.isSubmitting} type="submit">{form.formState.isSubmitting?<><LoaderCircle className="spin"/> Saving…</>:<>Finish setup <Check/></>}</button>}</div></form></div></div>{toast&&<div className="success-toast" role="status"><Check/> Profile complete! Taking you to your dashboard…</div>}</main>}


