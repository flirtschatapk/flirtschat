export function OnboardingProgress({step}:{step:number}){const percent=step*25;return <div className="onboarding-progress" aria-label={`Profile setup ${percent}% complete, step ${step} of 4`}><div className="progress-copy"><span>Profile setup · minimum 70% required</span><strong>{percent}%</strong></div><div className="onboarding-track">{[1,2,3,4].map(n=><i key={n} className={n<=step?"active":""}/>)}</div><div className="progress-labels"><span>Photos</span><span>About</span><span>Vibe</span><span>Preferences</span></div></div>}
