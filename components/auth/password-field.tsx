"use client";
import { useState } from "react";import { Eye,EyeOff,LockKeyhole } from "lucide-react";import type { UseFormRegisterReturn } from "react-hook-form";
export function PasswordField({registration,error,placeholder="Password",label="Password",autoComplete="current-password"}:{registration:UseFormRegisterReturn,error?:string,placeholder?:string,label?:string,autoComplete?:string}){const [visible,setVisible]=useState(false);return <div className="field-group"><label htmlFor={registration.name}>{label}</label><div className="auth-input"><LockKeyhole/><input id={registration.name} type={visible?"text":"password"} placeholder={placeholder} autoComplete={autoComplete} aria-invalid={Boolean(error)} {...registration}/><button type="button" onClick={()=>setVisible(!visible)} aria-label={visible?`Hide ${label.toLowerCase()}`:`Show ${label.toLowerCase()}`}>{visible?<EyeOff/>:<Eye/>}</button></div>{error&&<p className="field-error" role="alert">{error}</p>}</div>}
