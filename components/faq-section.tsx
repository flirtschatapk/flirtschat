"use client";
import { useState } from "react";import { ChevronDown } from "lucide-react";import { faqs } from "@/data/landing-page";
export function FaqSection(){const [open,setOpen]=useState(0);return <section id="faq" className="section faq-section"><div className="container faq-layout"><div className="faq-intro"><span className="kicker">Need to know</span><h2>Questions,<br/><em>meet answers.</em></h2><p>Still curious? Our support team is available any time.</p><a href="mailto:hello@flirtschat.example">Ask us anything ↗</a></div><div className="accordion">{faqs.map(([q,a],i)=><article className={open===i?"open":""} key={q}><button onClick={()=>setOpen(open===i?-1:i)} aria-expanded={open===i} aria-controls={`faq-${i}`}><span>{q}</span><ChevronDown/></button><div id={`faq-${i}`} className="answer" aria-hidden={open!==i}><p>{a}</p></div></article>)}</div></div></section>}
